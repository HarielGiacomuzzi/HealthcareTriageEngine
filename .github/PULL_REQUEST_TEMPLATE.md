<!-- One or two sentences: what this delivers, and what it deliberately does not. -->

<!-- Link the spec section this implements and the plan it was built from. -->
Implements [`specs/…`](specs/). Plan: [`docs/plans/…`](docs/plans/).

## Exit criteria

<!-- The phase's own "Done when" from specs/06-roadmap.md, one row each, with the observed result — not "should pass". -->

| Criterion | Result |
|---|---|
|  |  |

## What landed

<!-- One bullet per component, bolded name then what it does and why it is shaped that way.
     Name the ADR where one applies. State what a later phase replaces, so the reader knows
     which seams are deliberate. -->

-

## Review

<!-- How the work was reviewed, then the defects that were actually found and fixed — each one
     stated as the failure it would have caused, not as "addressed review feedback". A fix worth
     listing is one a reader would want to know shipped; skip the polish. -->

## Deliberately deferred

<!-- What a reader might expect to find here and will not, with the phase that owns it.
     Record any accepted deviation from the spec and what closes it. -->
